<h1>Songs by <%= @artist.full_name %><br></h1>
<% @artist.songs.each do |song| %>
         <li><%= link_to song.full_title, artist_song_path(@artist, song) %></li>
<% end %>
<%= new_button_for_admin %>
