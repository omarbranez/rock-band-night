<%= @artist.full_name %><br>
<%= @artist.songs.first.genre.name %><br>
<% @artist.songs.each do |song| %>
    <ul>
        <li><%= link_to song.full_title, artist_song_path(@artist, song) %>
    </ul>
<% end %>