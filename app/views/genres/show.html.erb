<h1><%= @genre.name %></h1>
<p>The following songs belong to this genre:</p>
<% @genre.songs.each do |song| %>
    <ul>
        <li><%= link_to song.full_title, artist_song_path(song.artist, song) %></li>
    </ul>
<% end %>